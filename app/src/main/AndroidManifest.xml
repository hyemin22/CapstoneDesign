<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.CapstoneDesign"
        tools:targetApi="31">
        <activity android:name=".AlbumDiaryList" />
        <activity android:name=".AlbumCreateActivity" />
        <activity android:name=".FeedEventFragment" />
        <activity android:name=".DiaryActivity" />
        <activity android:name=".GuestWriteActivity" />
        <activity android:name=".GuestBookCheckActivity" />
        <activity android:name=".PostWriteActivity" />
        <activity android:name=".PostCheckActivity" />
        <activity android:name=".AlarmActivity" />
        <activity android:name=".WishCreateActivity" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="fzwnh8umkh" />

        <activity
            android:name=".FeedActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupNameActivity" android:label="SignupName 액티비티"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".SignupIconActivity" android:label="SignupIcon 액티비티"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".SignupTermsActivity" android:label="SignupTerms 액티비티"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".SignupSuccessActivity" android:label="SignupSuccess 액티비티"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <activity android:name=".SignupInviteActivity" android:label="SignupInvite 액티비티"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
    </application>

</manifest>